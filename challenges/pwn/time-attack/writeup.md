```
        Ce challenge était hébergé sur un sevreur et accessible via SSH. Pour trouver le mot de passe, 
        il suffisait d'exécuter la commande :
        $> strings crack_me 
        étant donné que la version déployée était celle de debug (une erreur de notre part).
```
